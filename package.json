{
  "name": "paseo-productivity-plugins",
  "private": true,
  "workspaces": [
    "plugins/*"
  ],
  "devDependencies": {
    "@types/bun": "^1.2.22",
    "@types/node": "^24.3.0",
    "prettier": "^3.6.2"
  },
  "scripts": {
    "format": "prettier --write AGENTS.md README.md docs plugins package.json",
    "format:check": "prettier --check AGENTS.md README.md docs plugins package.json",
    "test": "bun test plugins",
    "typecheck": "bun run --filter '*' typecheck"
  }
}
